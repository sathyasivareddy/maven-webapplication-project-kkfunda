pipeline {
   agent any
   stages{
      stage('git checkout') {
         steps{
            git branch: 'development', url: 'https://github.com/sathyasivareddy/maven-webapplication-project-kkfunda/'
         }
      }
      stage('COMPILE') {
         steps{
           
         }
         stage('stage 2') {
         steps{
            echo 'sathya 2 '
         }
      }
   }
}
